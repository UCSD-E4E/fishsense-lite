# Commits that changed formatting only. `git blame` skips these when told to:
#
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# That setting is per-clone and cannot be committed, so run it once after
# cloning — otherwise blame on any of the 133 reformatted src files points at
# the reformat instead of the commit that wrote the line.

# style: format the packages' src trees with black, and gate it in CI
aa117f77f8d49c7ccfb3859bc581b2216bcecd5b
